/**
 * Optional aid: emit src/types/api/* from openapi.yaml to reduce hand-mirroring.
 * The YAML stays the source of truth; emitted files are committed + reviewed.
 * Real implementation lands in F00-T07 / F13-T01.
 */
// eslint-disable-next-line no-console
console.log("TODO(F13-T01): generate types/api/* from openapi.yaml.");
process.exit(0);
