/**
 * Create the first ADMIN staff user. Real implementation lands in F01-T02
 * (argon2id hash → User row). Usage: `pnpm create:admin -- <email> <name> <password>`.
 */
// eslint-disable-next-line no-console
console.log("TODO(F01-T02): hash password with argon2id and insert an ADMIN User.");
process.exit(0);
